{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "build": "mkdir -p build && echo 'Build step completed' > build/placeholder.txt",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.2",
    "@joggr/fastify-prisma": "^6.0.0",
    "@prisma/client": "^6.8.2",
    "build": "^0.1.4",
    "dotenv": "^16.5.0",
    "fastify": "^5.3.3",
    "fastify-type-provider-zod": "^4.0.2",
    "npm": "^11.4.1",
    "run": "^1.5.0"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "prisma": "^6.8.2"
  },
  "eslintConfig": {
    "extends": [
      "eslint:recommended"
    ]
  }
}
