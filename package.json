{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "fastify": "^4.23.0"
  },
  "devDependencies": {
    "prisma": "^6.8.2",
    "eslint": "^8.44.0"
  },
  "eslintConfig": {
    "extends": [
      "eslint:recommended"
    ]
  }
}
